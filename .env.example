# Porta em que o servidor escuta (Coolify define automaticamente)
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Supabase (banco externo blyx — já embutido no build via vite.config.ts,
# estas variáveis existem apenas para overrides em ambientes SSR/edge functions)
SUPABASE_URL=https://blyxvehtkkhmuqylashi.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_vbrH8T7FXYOfWGohGiuuiw_MS1SFiNF

# Necessário APENAS se você usar server functions que exigem service role
# (ex.: emails.functions.ts consulta a Admin API do Supabase).
# NUNCA exponha esta chave no client.
# SUPABASE_SERVICE_ROLE_KEY=
